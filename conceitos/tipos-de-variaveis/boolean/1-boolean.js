const isActive = true;

const isAuthenticated = false;

console.log(`\nO valor da variável "isActive" é: ${isActive} e o tipo é:`, typeof(isActive));
console.log(`\nO valor da variável "isAuthenticated" é: ${isAuthenticated} e o tipo é:`, typeof(isAuthenticated));

// Aqui a coisa é bem simples 
// O valor só pode ser true ou false
// E o tipo sempre será boolean